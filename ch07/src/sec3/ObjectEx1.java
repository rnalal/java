package sec3;
//함수와 객체 전달2
public class ObjectEx1 {
	public static void main(String[] args) {
		
		/*fnc0();
		
		Parent p1 = new Parent();
		p1.setLastName("전");
		p1.setFirstName("재영");
		p1.setAge(25);
		fnc1(p1);
		
		Parent p2 = fnc2();
		System.out.println(p2.getLastName()+p2.getFirstName()+p2.getAge());
		
		Parent pt = new Parent();		//전송할 데이터
		pt.setLastName("구");
		pt.setFirstName("미미");
		pt.setAge(7);
		Parent p3 = fnc3(pt);
		System.out.println(p3.getLastName()+p3.getFirstName()+p3.getAge());*/

	 }

}
		
		
		
		//fn1은 받기만 함
		//fn2는 주기만 함
		//fn3은 받고 줌
		
		/*
		*함수형태
		1. 줄것도 없고, 받을 것도 없음
		2. 받기만함 -> 매개변수 필요(String, int..)
		3. 주기만함(return) -> 반환타입(정수-int, 실수-double,float, 문자-string) 
		4. 주고 받음
		*/
